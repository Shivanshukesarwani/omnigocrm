@extends('layouts.app') @section('title','· Dashboard') @section('content')
<div class="page-head"><div><h1>Dashboard</h1><p class="muted">Your sales workspace at a glance.</p></div><a class="btn primary" href="{{ route('leads.create') }}">+ New Lead</a></div>
<div class="cards"><div class="card"><span>Leads</span><strong>{{ $leadCount }}</strong></div><div class="card"><span>Contacts</span><strong>{{ $contactCount }}</strong></div><div class="card"><span>Customers</span><strong>{{ $customerCount }}</strong></div><div class="card"><span>Follow-ups</span><strong>{{ $pendingFollowUps }}</strong></div></div>
<div class="two-col"><section class="panel"><div class="panel-head"><h2>Recent Leads</h2><a href="{{ route('leads.index') }}">View all</a></div><table><thead><tr><th>Name</th><th>Company</th><th>Status</th><th></th></tr></thead><tbody>@forelse($recentLeads as $lead)<tr><td>{{ $lead->first_name }} {{ $lead->last_name }}</td><td>{{ $lead->company ?: '—' }}</td><td><span class="badge">{{ $lead->status }}</span></td><td><a href="{{ route('leads.show',$lead) }}">Open</a></td></tr>@empty<tr><td colspan="4">No leads yet.</td></tr>@endforelse</tbody></table></section>
<section class="panel"><div class="panel-head"><h2>Recent Calls</h2></div><table><thead><tr><th>Phone</th><th>Duration</th><th>By</th></tr></thead><tbody>@forelse($recentCalls as $call)<tr><td>{{ $call->phone }}</td><td>{{ $call->durationLabel() }}</td><td>{{ $call->user->name }}</td></tr>@empty<tr><td colspan="3">No calls yet.</td></tr>@endforelse</tbody></table></section></div>
@endsection
