@extends('layouts.app')
@section('title','· Tasks')
@section('content')
<div class="page-head"><div><h1>Tasks</h1><p class="muted">Assign and complete sales work.</p></div></div>
<div class="two-col"><section class="panel"><h2>New task</h2><form method="post" action="{{ route('tasks.store') }}">@csrf<div class="form-grid"><div class="span-2"><label>Title<input name="title" required></label></div><div class="span-2"><label>Description<textarea name="description"></textarea></label></div><div><label>Due<input name="due_at" type="datetime-local"></label></div><div><label>Priority<select name="priority"><option>normal</option><option>low</option><option>high</option><option>urgent</option></select></label></div><div><label>Assign<select name="assigned_to"><option value="">Unassigned</option>@foreach($users as $u)<option value="{{ $u->id }}">{{ $u->name }}</option>@endforeach</select></label></div><div><label>Lead<select name="subject_id"><option value="">None</option>@foreach($leads as $l)<option value="{{ $l->id }}">{{ $l->first_name }} {{ $l->last_name }}</option>@endforeach</select></label></div></div><button class="btn primary">Create task</button></form></section><section class="panel"><h2>Tasks</h2><table><thead><tr><th>Task</th><th>Due</th><th>Priority</th><th>Status</th><th></th></tr></thead><tbody>@foreach($tasks as $t)<tr><td>{{ $t->title }}<br><span class="muted">{{ $t->description }}</span></td><td>{{ $t->due_at?->format('d M Y H:i') ?: '—' }}</td><td>{{ $t->priority }}</td><td>{{ $t->status }}</td><td>@if($t->status!=='completed')<form method="post" action="{{ route('tasks.complete',$t) }}">@csrf<button class="btn">Complete</button></form>@endif</td></tr>@endforeach</tbody></table>{{ $tasks->links() }}</section></div>
@endsection